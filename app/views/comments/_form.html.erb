<div class="text-right">
	<%= form_for([@product, @product.comments.build]) do |form| %>
	<%= form.label "Yorum Yap" %>
	<%= form.text_field :content %>
	<%= form.submit "Gönder", class: "btn btn-success" %>
	<% end %>
</div>
